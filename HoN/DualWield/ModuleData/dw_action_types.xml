<?xml version="1.0" encoding="utf-8"?>
<!--
  DualWield Mod - Action Types (MINIMAL)

  Unlike ROT which defines 76 custom act_dual_* action types,
  we reuse vanilla 1h action types directly in item_usage_sets.
  The require_left_hand_usage_root_set mechanism handles the
  dual-wield vs. solo selection without needing custom types.

  This file is intentionally minimal. Custom action types can be
  added later for combat_parameter tuning if needed.
-->
<action_types>
</action_types>
